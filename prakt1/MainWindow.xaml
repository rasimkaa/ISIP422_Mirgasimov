<Window x:Class="prakt1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Учёт товаров" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="Добавить" Width="100" Margin="5" Click="AddProduct_Click"/>
            <Button Content="Удалить" Width="100" Margin="5" Click="DeleteProduct_Click"/>
            <Button Content="Продать" Width="100" Margin="5" Click="SellProduct_Click"/>
            <Button Content="Заказать" Width="100" Margin="5" Click="RestockProduct_Click"/>
            <Button Content="Отмена продажи" Width="120" Margin="5" Click="UndoLastSale_Click"/>
            <Button Content="Отчёт о продажах" Width="140" Margin="5" Click="SalesReport_Click"/>
        </StackPanel>

        <!-- Таблица товаров -->
        <DataGrid x:Name="ProductsGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Код" Binding="{Binding Code}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="100"/>
                <DataGridCheckBoxColumn Header="В наличии" Binding="{Binding InStock, Mode=OneWay}" Width="100"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
